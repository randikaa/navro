import type { Metadata } from "next";
import { Inter, Playfair_Display } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

const playfair = Playfair_Display({
  subsets: ["latin"],
  variable: "--font-playfair",
});

export const metadata: Metadata = {
  title: "Navro Seafood Family Restaurant | Panadura",
  description: "Experience the finest Indian and Sri Lankan seafood at Navro Seafood Family Restaurant in Panadura. Fresh catches, authentic flavors, and family traditions since 2015.",
  keywords: ["seafood restaurant", "Panadura", "Indian cuisine", "family restaurant", "Navro", "Sri Lanka"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${inter.variable} ${playfair.variable} h-full antialiased`}>
      <body className="min-h-full flex flex-col bg-[#0a0a0a]">{children}</body>
    </html>
  );
}
